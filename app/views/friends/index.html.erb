<% content_for :title, "Friends" %>
<h1>Friends</h1>
<br/>
  <%= link_to "New friend", new_friend_path, style: "text-decoration: none; color:black;", class: "btn btn-outline-primary"%>
<br><br/>
<table class="table table-striped table-bordered table-hover">
  <thead class="table-dark">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Twitter</th>
    </tr>
  </thead>
  <tbody>
    <% @friends.each do |friend| %>
      <tr>
        <td>
          <%= link_to friend.first_name + ' ' + friend.last_name, friend %>
        </td>
        <td><%= friend.email %></td>
        <td><%= friend.phone %></td>
        <td><%= friend.twitter %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
    
  </div>
</div>

    